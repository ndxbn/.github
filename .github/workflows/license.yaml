name: license validation
on:
  push:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    environment: ci
    permissions: read-all
    steps:
      - run: gh repo ls --no-archived --limit 300 --json name --jq ".[].name"
