name: license validation
on:
  push:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    permissions: read-all
    env:
      GH_TOKEN: "${{ github.token }}"
    steps:
      - run: gh repo ls --no-archived --limit 300 --json name --jq ".[].name" | xargs -L 1 echo
